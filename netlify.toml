## Netlify configuration for Vite React SPA
[build]
  command = "npm run build"
  publish = "dist"

## Ensure client-side routing works on refresh (SPA rewrite)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200